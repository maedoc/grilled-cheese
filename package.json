{
  "name": "grilled-cheese",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "build:lib": "BUILD_LIB=1 vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "vite",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tonaljs/tonal": "^4.10.0",
    "chord-symbol": "^4.0.0"
  },
  "devDependencies": {
    "@tsconfig/strictest": "^2.0.5",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
